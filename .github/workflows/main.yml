name: CI
on:
  push:
    branches: [ master ]
jobs:
  container-test-job:
    runs-on: ubuntu-latest
    container:
      image: php:7.0-cli
      env:
        NODE_ENV: development
    steps:
      - name: Build phar
        run: 

